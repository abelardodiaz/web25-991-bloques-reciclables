[project]
name = "api-simple"
version = "0.1.0"
description = "Simple API example using ulfblk-core"
requires-python = ">=3.11"
dependencies = [
    "ulfblk-core",
    "uvicorn>=0.30",
]

[tool.uv.sources]
ulfblk-core = { workspace = true }
